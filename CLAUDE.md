# Claude Code Project Memory — 하찮력

## 1. 제품 정의

이 프로젝트는 앱인토스에서 실행되는 **비게임 행동 검사 앱 `하찮력`**이다.

사용자는 첫날 약 90~120초 동안 5개의 짧은 행동 검사를 수행하고, 이후 서로 다른 날짜의 짧은 추가 분석을 거쳐 7번째 분석일에 최종 사용설명서를 완성한다.

1. 시간 감각
2. 중심 인지
3. 균형 분배
4. 손가락 통제
5. 시각 집중

검사 결과로 실제 측정값, 능력별 점수, 행동 특성, 종합 유형, 가상 자격을 제공한다.

## 2. 고정 기술 범위

- React
- TypeScript strict mode
- Apps in Toss WebView SDK 3.x 계열
  (@apps-in-toss/web-framework 3.0.2)
- TDS 우선 사용
- 서버 없음
- 외부 DB 없음
- 로그인 없음
- 광고·결제 없음
- 기록은 기기 로컬에만 저장

## 3. 제품 범위에서 제외

다음 기능을 제안하거나 구현하지 않는다.

- Supabase 및 기타 외부 DB
- 전국 랭킹 또는 실제 백분위
- 가짜 이용자 수 또는 가짜 순위
- 친구 목록과 온라인 대결
- 게임 스테이지, 생명, 보상, 아이템
- 게시판, 채팅, 사용자 업로드
- AI API 기반 결과 생성
- 결제, 광고, 토스포인트

## 4. UX 원칙

- 첫 화면에서 앱 목적을 5초 안에 이해할 수 있어야 한다.
- 첫 검사 시작까지 10초를 넘기지 않는다.
- 전체 종합검사는 90~120초를 목표로 한다.
- 한 화면에는 핵심 행동 하나만 제공한다.
- 검사 진행 중 불필요한 탭·메뉴·분기를 제공하지 않는다.
- 결과는 실제 원시 측정값을 사용자에게 보여준다.
- 낮은 점수도 모욕 없이 건조한 유머로 해석한다.
- 결과 화면과 공유 카드가 앱의 가장 중요한 산출물이다.

## 5. 코드 원칙

- 검사 UI, 원시 결과, 점수 계산, 유형 판정을 분리한다.
- 점수 함수는 순수 함수로 작성한다.
- TypeScript `any` 사용을 피한다.
- 시간 측정에는 `performance.now()`를 우선한다.
- 앱이 백그라운드로 전환되면 진행 중인 시간 기반 시도는 무효화한다.
- 기기 크기 영향을 받는 측정값은 픽셀 절대값이 아니라 비율로 정규화한다.
- 로컬 저장 데이터에는 반드시 `schemaVersion`을 포함한다.
- 기존 명세를 바꾸는 리팩터링은 사용자 승인 없이 하지 않는다.

## 6. 앱인토스 원칙

- Apps in Toss 관련 API는 공식 MCP 또는 docs-search로 확인한다.
- 존재 여부를 추측해 SDK 함수를 만들지 않는다.
- 비게임 출시 가이드와 TDS 사용 기준을 준수한다.
- `apps-in-toss.config.ts` 설정은 프로젝트 실제 값과 일치시킨다.

## 7. 작업 방식

모든 작업은 다음 순서로 진행한다.

1. `HANDOFF.md`, `templates/PROGRESS.md`, 이 파일과 관련 `docs/` 문서를 읽는다.
2. 수정 전 계획, 대상 파일, 테스트 계획을 제시한다.
3. 요청된 단계 범위만 구현한다.
4. 새 패키지가 필요하면 설치 전에 이유와 대안을 설명한다.
5. 구현 후 typecheck, lint, test, build를 실행한다.
6. 실패를 숨기지 않고 정확히 보고한다.
7. 변경 파일, 핵심 설계, 남은 문제를 요약한다.
8. `HANDOFF.md`와 `templates/PROGRESS.md`를 갱신한다.
9. 다른 AI 도구가 이어서 작업할 수 있도록 결정사항을 세션 안에만 남기지 않는다.

## 8. 완료 기준

기능 완료를 주장하려면 최소한 다음이 충족되어야 한다.

- TypeScript 오류 없음
- 린트 오류 없음
- 관련 단위 테스트 통과
- 프로덕션 빌드 성공
- 360px 폭 모바일 화면에서 핵심 CTA가 잘리지 않음
- 새 기능의 오류·중단·재시도 흐름이 존재
