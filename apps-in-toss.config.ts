import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'hachannyeok',
  brand: {
    primaryColor: '#103F38',
  },
  permissions: [],
  webBundleDir: 'dist',
});
