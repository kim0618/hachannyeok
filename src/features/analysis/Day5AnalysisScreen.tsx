import{PrimaryButton}from'../../components/PrimaryButton';import{DailyDiscoveryPanel}from'./DailyDiscoveryPanel';import type{ControlConditionTrial}from'../../domain/assessment/trials';import type{DerivedAnalysis}from'../../domain/scoring/types';import type{DailyRecord}from'../../domain/storage/types';interface Props{record:DailyRecord;before:DerivedAnalysis;after:DerivedAnalysis;saveStatus:'saving'|'saved'|'failed';onRetrySave?:()=>void;onHome?:()=>void;onAnalysis?:()=>void}const mean=(v:number[])=>v.reduce((s,n)=>s+n,0)/v.length,pct=(v:number)=>`${(v*100).toFixed(1)}%`,delta=(v:number)=>`${v>=0?'+':''}${(v*100).toFixed(1)}%p`;
export function Day5AnalysisScreen({record,before,after,saveStatus,onRetrySave,onHome,onAnalysis}:Props){const trials=record.rawResult.assessmentType==='day5_control_surprise'?record.rawResult.trials.filter((t):t is Extract<ControlConditionTrial,{valid:true}> =>t.valid):[];const errors=(c:'predictable'|'surprise')=>trials.filter(t=>t.condition===c).map(t=>Math.abs(t.observedPosition-t.targetPosition));const p=errors('predictable'),s=errors('surprise');if(!p.length||!s.length)return <div className="screen analysis-error"><section role="alert"><h1>분석에 필요한 기록을 계산하지 못했습니다.</h1></section></div>;const pm=mean(p),sm=mean(s),change=sm-pm,t=after.tendencies.find(x=>x.key==='surpriseSensitivity'),eligible=Boolean(t?.eligible),headline=!eligible?'갑작스러운 변속에도 통제력은 거의 그대로':change>0?'예상이 깨지면 정지 오차가 커지는 편':'갑작스러운 변속에서도 오차가 줄어드는 편',deltaLabel=!eligible?'변화 거의 없음':change>0?'오차 증가':change<0?'오차 감소':'변화 없음',scoreChanged=before.scores.control!==after.scores.control;return <div className="screen analysis-screen daily-analysis day5-analysis"><header className="analysis-record-header"><span>쓸능검 · 추가 분석</span><span>DAY 5</span></header><main><DailyDiscoveryPanel day={5} insight={headline} nextTeaser="DAY 6에는 위치가 가까이 모일 때 기억 정확도가 어떻게 달라지는지 확인해요."><span className="analysis-chip">심화 분석 4/5</span></DailyDiscoveryPanel><section className="report-section"><div className="section-index">01 · SURPRISE CONTROL</div><h2>예측 가능 조건과 돌발 변속 비교</h2><div className="condition-cards"><div><span>예측 가능한 움직임</span><strong>{pct(pm)}</strong><small>평균 정지 오차</small></div><div><span>갑작스러운 변속</span><strong>{pct(sm)}</strong><small>평균 정지 오차</small></div></div><div className="condition-delta"><span>{deltaLabel}</span><strong>{delta(change)}</strong></div></section><section className="report-section score-secondary"><div className="section-index">02 · CONTROL SCORE</div><h2>손가락 통제 점수</h2>{scoreChanged?<p className="score-change">{before.scores.control} → {after.scores.control}</p>:<p>손가락 통제 점수는 그대로 유지됐어요.</p>}<p>시간·중심·균형·집중 점수는 기존 결과를 유지합니다.</p></section>{saveStatus==='saving'&&<p role="status" className="storage-status">추가 분석을 기기에 저장하고 있습니다.</p>}{saveStatus==='failed'&&<div role="alert" className="storage-warning"><p>추가 분석을 저장하지 못했습니다. 측정 결과는 그대로 유지됩니다.</p>{onRetrySave&&<button className="secondary-button" onClick={onRetrySave}>다시 저장</button>}</div>}<div className="analysis-actions"><PrimaryButton disabled={!onHome} onClick={onHome}>홈으로</PrimaryButton>{onAnalysis&&<button className="secondary-button" onClick={onAnalysis}>업데이트된 분석서 보기</button>}</div></main></div>}
