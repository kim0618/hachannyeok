export function NextAssessmentPlaceholder() {
  return (
    <div className="screen placeholder-screen">
      <section aria-labelledby="next-title">
        <p className="eyebrow">검사 2 / 5</p>
        <h1 id="next-title">다음 측정 준비 중</h1>
        <p>중심 인지 검사는 다음 단계에서 연결됩니다.</p>
      </section>
    </div>
  );
}
